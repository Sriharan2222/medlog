{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd backend && npm ci && npx prisma generate && npx prisma db push && cd ../frontend && npm ci && NEXT_PUBLIC_API_URL=https://medlog-production.up.railway.app/api npm run build"
    },
    "deploy": {
        "startCommand": "cd backend && node src/index.js",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}